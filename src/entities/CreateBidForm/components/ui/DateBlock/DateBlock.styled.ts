import { Button } from 'shared/components/ui';
import styled from 'styled-components';

export const DateBlock = styled.div`
  position: relative;
  display: flex;
`;

export const OpenCalBtn = styled(Button)`
  position: relative;
`;
